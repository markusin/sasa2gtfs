DROP TABLE @TABLE_NAME@;
CREATE TABLE @TABLE_NAME@ AS (
 	SELECT 
  	TRIP_ID
		, STOP_ID
		, DEPARTURE_TIME AS ARRIVAL_TIME
		, TIMESTAMP2SECONDSAFTERMDN(DEPARTURE_TIME,'YYYY-MM-DD') AS TIME_A
		, DEPARTURE_TIME
		, TIMESTAMP2SECONDSAFTERMDN(DEPARTURE_TIME,'YYYY-MM-DD') AS TIME_D
		, STOP_SEQUENCE 
 	FROM @TMP_TABLE_NAME@
);
ALTER TABLE @TABLE_NAME@ ADD PRIMARY KEY (TRIP_ID,STOP_ID,STOP_SEQUENCE);
QUIT
