INSERT INTO @TABLE_NAME@ (ID, SOURCE, TARGET, LENGTH, GEOMETRY,EDGE_MODE) 
  SELECT 
	@SEQUENCE_NAME@.NEXTVAL,L.TARGET,L.SOURCE,L.LENGTH,SDO_LRS.REVERSE_GEOMETRY(L.GEOMETRY),0 
  FROM 
    @TABLE_NAME@ L;
COMMIT;
QUIT